#!/bin/bash
# Recover data/jpg/<Photos-YY>/<trip>/*.jpg from the flat Lightroom export in
# data/jpg/_flat (filenames encoded as <trip>~~<filename>.jpg). Dry run by
# default; pass --apply to actually move files.
cd "$(dirname "$0")"
[ -d .venv ] && source .venv/bin/activate
python3 tools/decode_flat_jpg.py "$@"
