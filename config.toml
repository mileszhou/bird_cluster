# Non-secret, checked-in project configuration. Secrets (API keys) stay in .env
# (gitignored) — this file is for anything safe to commit, starting with which
# host each backend server runs on. Add new parameters here as they come up.

# Which run directory the analysis tools work on by default. Runs get renamed —
# ./clean archives output/ to output_NNN_<description>/ — so the post-processors
# must be told where to look rather than assuming ./output. Point this at an
# archive to re-analyse it, and every tool follows without a flag.
current_working_output = "./output"

# The dataset. Left unset on purpose: resolution is by existence, so a working
# checkout finds ./data (the private submodule) and a fresh clone finds the
# shipped ./sample_data without either editing this file. Set it only for a
# library that lives elsewhere -- or use $BIRD_DATA_DIR, which does not dirty
# the tree.
# data_dir = "/mnt/photos/library"

[servers.vllm]
host = "darwin"
port = 8000

[servers.embed]
host = "spark"
port = 9100

[servers.llama_cpp]
host = "darwin"
port = 8080
