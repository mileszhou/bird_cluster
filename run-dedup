#!/bin/bash
# Find sidecars describing the same photo across the unsplit XMP library.
# Read-only; writes the report and CSVs into project/reports/.
cd "$(dirname "$0")"
[ -d .venv ] && source .venv/bin/activate
python3 tools/dedup_sidecars.py --xmp-dir ./data/xmp0 "$@"
